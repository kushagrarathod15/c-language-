#include <stdio.h>

int main() {
    int num;

    printf("Enter an integer: ");
    scanf("%d", &num);

    // Using bitwise AND and ternary operator to check even/odd
    (num & 1)? printf("The number is odd.\n"): printf("The number is even.\n");

    return 0;
}
